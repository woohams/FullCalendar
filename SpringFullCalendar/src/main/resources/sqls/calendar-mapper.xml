<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="calendar">

	<select id="selectList" resultType="calendarDto">
		SELECT CALENDAR_SEQ, ID, CALENDAR_CATEGORY, CALENDAR_TITLE, CALENDAR_CONTENT, 
		CALENDAR_BACKGROUNDCOLOR, CALENDAR_START, CALENDAR_END, CALENDAR_ALLDAY
		FROM CALENDAR
	</select>
	
	<select id="selectOne" parameterType="int" resultType="calendarDto">
		SELECT CALENDAR_SEQ, ID, CALENDAR_CATEGORY, CALENDAR_TITLE, CALENDAR_CONTENT, 
		CALENDAR_BACKGROUNDCOLOR, CALENDAR_START, CALENDAR_END, CALENDAR_ALLDAY
		FROM CALENDAR
		WHERE CALENDAR_SEQ = #{calendar_seq}
	</select>
	
	<insert id="insert" parameterType="calendarDto">
		INSERT INTO CALENDAR VALUES(CALENDAR_SEQ.NEXTVAL, #{id}, 
		#{calendar_title}, #{calendar_content}, #{calendar_backgroundColor}, 
		#{calendar_start}, #{calendar_end})
	</insert>

	<update id="update" parameterType="calendarDto">
		UPDATE CALENDAR SET CALENDAR_TITLE = #{calendar_title}, CALENDAR_CATEGORY = #{calendar_category},
		CALENDAR_CONTENT = #{calendar_content}, CALENDAR_BACKGROUNDCOLOR = #{calendar_backgroundColor},
		CALENDAR_START = #{calendar_start}, CALENDAR_END = #{calendar_end}, CALENDAR_ALLDAY = #{calendar_allDay}
		WHERE ID = #{id}
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE CALENDAR WHERE CALENDAR_SEQ = #{calendar_seq}
	</delete>
	
	
</mapper>




